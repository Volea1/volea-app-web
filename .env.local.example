# The static export talks to `/api` on the same host (nginx → volea-app-api).
# For `next dev` without the docker/nginx proxy, point rewrites at the BFF:
# VOLEA_DEV_API_URL=http://volea-app-api:8180
